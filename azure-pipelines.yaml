---
trigger:
- main

pool: NTR

stages:
- stage: image
  jobs:
  - job: imagebuild
    steps:
    - task: Docker@2
      inputs:
        containerRegistry: 'dockerhubnew'
        repository: 'madhutony'
        command: 'buildAndPush'
        Dockerfile: './Dockerfile'
        tags: 'saleor'